<div>
    {{#each posts as |post|}}
    <div>
        <h2>
            <a href="/post/{{id}}">{{title}}</a>
        </h2>
        <div>
            Posted by {{post.user.name}} on {{format_date createdAt}}
        </div>
        <div>
            {{body}}
        </div>
    </div>
    {{/each}}
</div>